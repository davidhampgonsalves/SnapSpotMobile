'use strict'

const trip = ['trip']

const positions = ['trip', 'positions']

const status = ['trip', 'status']

const remainingMinutes = ['trip', 'remainingMinutes']

const tripErrors = ['trip', 'tripErrors']

export default { trip, positions, status, tripErrors, remainingMinutes }
